You are a senior full-stack engineer.

Build this project using Docker Compose and a clean modular architecture.

Before coding, read every file inside /docs.

Main requirements:

- Use Docker Compose.
- Use a monorepo structure with /frontend, /backend and /docs.
- Backend must use Node.js + Express + TypeScript.
- Frontend must use Next.js + TypeScript + TailwindCSS.
- Database must use PostgreSQL.
- Follow the structure defined in docs/project-structure.md.
- Follow the API defined in docs/api-spec.md.
- Follow the schema defined in docs/database-schema.sql.
- Follow the Docker setup defined in docs/docker.md.
- Implement JWT authentication.
- Implement role-based access control.
- Keep business rules inside services.
- Keep SQL queries inside repositories.
- Keep frontend API calls inside services.
- Use Portuguese labels in the interface.
- Use English names for code files, variables and folders.

The project must run with:

docker compose up --build

Generate clean, modular, maintainable and documented code.